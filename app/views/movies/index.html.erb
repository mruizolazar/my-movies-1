<h1 class="title">Movies</h1>

<table id="movies">
  <thead>
    <tr>
      <th>Title</th>
      <th>Release</th>
    </tr>
  </thead>
  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.release_date %></td>
      </tr>
    <% end %>
  </tbody>
</table>
